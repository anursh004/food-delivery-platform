server:
  port: 8083
spring:
  application:
    name: catalog-service
  datasource:
    url: jdbc:postgresql://postgres:5432/fooddb
    username: food
    password: foodpw
  jpa:
    hibernate:
      ddl-auto: validate
  cache:
    type: caffeine
  data:
    redis:
      host: redis
      port: 6379
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
